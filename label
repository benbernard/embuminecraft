args = {...}

sorter = peripheral.wrap("bottom")
print(textutils.serialize(sorter.list(2)))
print(textutils.serialize(sorter.list(3)))
print(textutils.serialize(sorter.list(4)))
print(textutils.serialize(sorter.list(5)))

for i,str in ipars(args) do

end